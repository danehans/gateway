- name: first-listener
  virtualHosts:
  - domains:
    - '*'
    name: first-listener
    routes:
    - match:
        path: foo/bar
      route:
        cluster: first-route
    - match:
        path: foo/baz
      route:
        cluster: second-route
